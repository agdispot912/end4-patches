diff --git a/assets/icons/codex-color.svg b/assets/icons/codex-color.svg
new file mode 100644
index 0000000..9ce4392
--- /dev/null
+++ b/assets/icons/codex-color.svg
@@ -0,0 +1 @@
+<svg height="1em" style="flex:none;line-height:1" viewBox="0 0 24 24" width="1em" xmlns="http://www.w3.org/2000/svg"><title>Codex</title><path d="M19.503 0H4.496A4.496 4.496 0 000 4.496v15.007A4.496 4.496 0 004.496 24h15.007A4.496 4.496 0 0024 19.503V4.496A4.496 4.496 0 0019.503 0z" fill="#fff"></path><path d="M9.064 3.344a4.578 4.578 0 012.285-.312c1 .115 1.891.54 2.673 1.275.01.01.024.017.037.021a.09.09 0 00.043 0 4.55 4.55 0 013.046.275l.047.022.116.057a4.581 4.581 0 012.188 2.399c.209.51.313 1.041.315 1.595a4.24 4.24 0 01-.134 1.223.123.123 0 00.03.115c.594.607.988 1.33 1.183 2.17.289 1.425-.007 2.71-.887 3.854l-.136.166a4.548 4.548 0 01-2.201 1.388.123.123 0 00-.081.076c-.191.551-.383 1.023-.74 1.494-.9 1.187-2.222 1.846-3.711 1.838-1.187-.006-2.239-.44-3.157-1.302a.107.107 0 00-.105-.024c-.388.125-.78.143-1.204.138a4.441 4.441 0 01-1.945-.466 4.544 4.544 0 01-1.61-1.335c-.152-.202-.303-.392-.414-.617a5.81 5.81 0 01-.37-.961 4.582 4.582 0 01-.014-2.298.124.124 0 00.006-.056.085.085 0 00-.027-.048 4.467 4.467 0 01-1.034-1.651 3.896 3.896 0 01-.251-1.192 5.189 5.189 0 01.141-1.6c.337-1.112.982-1.985 1.933-2.618.212-.141.413-.251.601-.33.215-.089.43-.164.646-.227a.098.098 0 00.065-.066 4.51 4.51 0 01.829-1.615 4.535 4.535 0 011.837-1.388zm3.482 10.565a.637.637 0 000 1.272h3.636a.637.637 0 100-1.272h-3.636zM8.462 9.23a.637.637 0 00-1.106.631l1.272 2.224-1.266 2.136a.636.636 0 101.095.649l1.454-2.455a.636.636 0 00.005-.64L8.462 9.23z" fill="url(#lobe-icons-codex-_R_0_)"></path><defs><linearGradient gradientUnits="userSpaceOnUse" id="lobe-icons-codex-_R_0_" x1="12" x2="12" y1="3" y2="21"><stop stop-color="#B1A7FF"></stop><stop offset=".5" stop-color="#7A9DFF"></stop><stop offset="1" stop-color="#3941FF"></stop></linearGradient></defs></svg>
diff --git a/modules/common/widgets/StyledPopup.qml b/modules/common/widgets/StyledPopup.qml
index d4b8275..0579d8b 100644
--- a/modules/common/widgets/StyledPopup.qml
+++ b/modules/common/widgets/StyledPopup.qml
@@ -11,6 +11,7 @@ LazyLoader {
     property Item hoverTarget
     default property Item contentItem
     property real popupBackgroundMargin: 0
+    property real popupContentWidth: 0
     active: hoverTarget && hoverTarget.containsMouse
 
     readonly property bool barVertical: Config.options.bar.vertical
@@ -32,7 +33,8 @@ LazyLoader {
         anchors.top: root.barEdge !== "bottom"
         anchors.bottom: root.barEdge === "bottom"
 
-        implicitWidth: popupBackground.implicitWidth + Appearance.sizes.elevationMargin * 2 + root.popupBackgroundMargin
+        implicitWidth: (root.popupContentWidth > 0 ? root.popupContentWidth : popupBackground.implicitWidth)
+            + Appearance.sizes.elevationMargin * 2 + root.popupBackgroundMargin
         implicitHeight: popupBackground.implicitHeight + Appearance.sizes.elevationMargin * 2 + root.popupBackgroundMargin
 
         readonly property real centerOffsetX: {
@@ -94,7 +96,9 @@ LazyLoader {
             }
 
             // Use local reference instead of crossing LazyLoader scope boundary
-            implicitWidth: (popupWindow.innerContent?.implicitWidth ?? 0) + margin * 2
+            implicitWidth: root.popupContentWidth > 0
+                ? root.popupContentWidth
+                : (popupWindow.innerContent?.implicitWidth ?? 0) + margin * 2
             implicitHeight: (popupWindow.innerContent?.implicitHeight ?? 0) + margin * 2
 
             color: Appearance.colors.colLayer1Base
@@ -111,4 +115,4 @@ LazyLoader {
             }
         }
     }
-}
\ No newline at end of file
+}
diff --git a/modules/ii/bar/CodexUsage.qml b/modules/ii/bar/CodexUsage.qml
new file mode 100644
index 0000000..1b46145
--- /dev/null
+++ b/modules/ii/bar/CodexUsage.qml
@@ -0,0 +1,153 @@
+import QtQuick
+import QtQuick.Layouts
+import Quickshell
+import Quickshell.Io
+import qs.modules.common
+import qs.modules.common.widgets
+
+MouseArea {
+    id: root
+
+    property bool vertical: Config.options.bar.vertical
+    property bool holding: false
+    property int usedPercent: -1
+    property string resetsAt: ""
+    property int weeklyUsedPercent: -1
+    property string weeklyResetsAt: ""
+    property double now: Date.now()
+
+    readonly property bool available: usedPercent >= 0 && resetsAt.length > 0
+    readonly property string timeUntilReset: formatDuration(new Date(resetsAt).getTime() - now)
+    readonly property string weeklyTimeUntilReset: formatDuration(new Date(weeklyResetsAt).getTime() - now)
+
+    implicitWidth: vertical ? Appearance.sizes.verticalBarWidth : content.implicitWidth + 10
+    implicitHeight: vertical ? content.implicitHeight + 8 : Appearance.sizes.barHeight
+    hoverEnabled: true
+    cursorShape: Qt.PointingHandCursor
+
+    function formatDuration(milliseconds) {
+        const totalMinutes = Math.max(0, Math.ceil(milliseconds / 60000))
+        const days = Math.floor(totalMinutes / 1440)
+        const hours = Math.floor(totalMinutes % 1440 / 60)
+        const minutes = totalMinutes % 60
+        if (days > 0) return `${days}d${hours}h`
+        if (hours > 0) return `${hours}h${minutes}m`
+        return `${minutes}m`
+    }
+
+    function parseUsage(text) {
+        try {
+            const accounts = JSON.parse(text)
+            const account = accounts.find(item => item.provider === "codex")
+            const primary = account?.usage?.primary
+            if (!primary || !Number.isFinite(Number(primary.usedPercent)) || !primary.resetsAt)
+                throw new Error("Codex 5-hour usage is unavailable")
+
+            usedPercent = Math.max(0, Math.min(100, Math.round(Number(primary.usedPercent))))
+            resetsAt = primary.resetsAt
+            const secondary = account.usage.secondary
+            weeklyUsedPercent = Number.isFinite(Number(secondary?.usedPercent))
+                ? Math.max(0, Math.min(100, Math.round(Number(secondary.usedPercent)))) : -1
+            weeklyResetsAt = secondary?.resetsAt ?? ""
+        } catch (error) {
+            usedPercent = -1
+            resetsAt = ""
+            weeklyUsedPercent = -1
+            weeklyResetsAt = ""
+        }
+    }
+
+    onPressed: holding = true
+    onReleased: holding = false
+    onCanceled: holding = false
+    onClicked: usageProcess.running = true
+
+    Timer {
+        interval: 60000
+        running: true
+        repeat: true
+        triggeredOnStart: true
+        onTriggered: {
+            root.now = Date.now()
+            if (!usageProcess.running) usageProcess.running = true
+        }
+    }
+
+    Process {
+        id: usageProcess
+        command: ["codexbar", "--json"]
+        stdout: StdioCollector {
+            onStreamFinished: root.parseUsage(text)
+        }
+        onExited: exitCode => {
+            if (exitCode !== 0) root.parseUsage("")
+        }
+    }
+
+    RowLayout {
+        id: content
+        visible: !root.vertical
+        anchors.centerIn: parent
+        spacing: 4
+
+        Image {
+            source: Quickshell.shellPath("assets/icons/codex-color.svg")
+            sourceSize.width: Appearance.font.pixelSize.normal
+            sourceSize.height: Appearance.font.pixelSize.normal
+            Layout.preferredWidth: Appearance.font.pixelSize.normal
+            Layout.preferredHeight: Appearance.font.pixelSize.normal
+        }
+
+        StyledText {
+            font.pixelSize: Appearance.font.pixelSize.smaller
+            font.features: { "tnum": 1 }
+            color: Appearance.colors.colOnLayer1
+            text: root.available ? `${root.usedPercent} · ${root.timeUntilReset}` : "—"
+        }
+    }
+
+    ColumnLayout {
+        visible: root.vertical
+        anchors.centerIn: parent
+        spacing: 1
+
+        Image {
+            Layout.alignment: Qt.AlignHCenter
+            source: Quickshell.shellPath("assets/icons/codex-color.svg")
+            sourceSize.width: Appearance.font.pixelSize.normal
+            sourceSize.height: Appearance.font.pixelSize.normal
+            Layout.preferredWidth: Appearance.font.pixelSize.normal
+            Layout.preferredHeight: Appearance.font.pixelSize.normal
+        }
+
+        StyledText {
+            Layout.alignment: Qt.AlignHCenter
+            font.pixelSize: Appearance.font.pixelSize.smallest
+            font.features: { "tnum": 1 }
+            color: Appearance.colors.colOnLayer1
+            text: root.available ? root.usedPercent : "—"
+        }
+
+        StyledText {
+            Layout.alignment: Qt.AlignHCenter
+            visible: root.available
+            font.pixelSize: Appearance.font.pixelSize.smallest
+            color: Appearance.colors.colOnSurfaceVariant
+            text: root.timeUntilReset
+        }
+    }
+
+    Item {
+        id: holdTarget
+        anchors.fill: parent
+        property bool containsMouse: root.holding
+    }
+
+    CodexUsagePopup {
+        hoverTarget: holdTarget
+        usedPercent: root.usedPercent
+        resetsAt: root.resetsAt
+        weeklyUsedPercent: root.weeklyUsedPercent
+        weeklyResetsAt: root.weeklyResetsAt
+    }
+}
diff --git a/modules/ii/bar/CodexUsagePopup.qml b/modules/ii/bar/CodexUsagePopup.qml
new file mode 100644
index 0000000..f305e61
--- /dev/null
+++ b/modules/ii/bar/CodexUsagePopup.qml
@@ -0,0 +1,101 @@
+import QtQuick
+import QtQuick.Layouts
+import qs.modules.common
+import qs.modules.common.widgets
+
+StyledPopup {
+    id: root
+    property int popupWidth: 240
+    readonly property int contentWidth: popupWidth - 16
+    readonly property int limitRowWidth: contentWidth - 16
+    popupContentWidth: popupWidth
+
+    property int usedPercent: -1
+    property string resetsAt: ""
+    property int weeklyUsedPercent: -1
+    property string weeklyResetsAt: ""
+
+    function formatDuration(milliseconds) {
+        const totalMinutes = Math.max(0, Math.ceil(milliseconds / 60000))
+        const days = Math.floor(totalMinutes / 1440)
+        const hours = Math.floor(totalMinutes % 1440 / 60)
+        const minutes = totalMinutes % 60
+        if (days > 0) return `${days}d ${hours}h`
+        if (hours > 0) return `${hours}h ${minutes}m`
+        return `${minutes}m`
+    }
+
+    component LimitRow: ColumnLayout {
+        required property string iconName
+        required property string title
+        required property string detail
+
+        Layout.minimumWidth: root.limitRowWidth
+        Layout.preferredWidth: root.limitRowWidth
+        Layout.maximumWidth: root.limitRowWidth
+        implicitWidth: root.limitRowWidth
+        spacing: 2
+
+        RowLayout {
+            spacing: 5
+
+            MaterialSymbol {
+                text: parent.parent.iconName
+                iconSize: Appearance.font.pixelSize.normal
+                color: Appearance.colors.colOnSurfaceVariant
+            }
+
+            StyledText {
+                text: parent.parent.title
+                font.pixelSize: Appearance.font.pixelSize.small
+                color: Appearance.colors.colOnSurfaceVariant
+            }
+        }
+
+        StyledText {
+            Layout.leftMargin: Appearance.font.pixelSize.normal + 5
+            text: parent.detail
+            font.pixelSize: Appearance.font.pixelSize.smaller
+            color: Appearance.colors.colOnSurfaceVariant
+            opacity: 0.7
+        }
+    }
+
+    ColumnLayout {
+        width: root.contentWidth
+        implicitWidth: root.contentWidth
+        spacing: 8
+
+        StyledText {
+            text: "Codex usage"
+            font.pixelSize: Appearance.font.pixelSize.normal
+            font.weight: Font.Medium
+            color: Appearance.colors.colOnSurfaceVariant
+        }
+
+        GroupedList {
+            Layout.fillWidth: true
+            bgcolor: Appearance.colors.colSurfaceContainerLow
+            itemVerticalPadding: 14
+
+            LimitRow {
+                Layout.fillWidth: true
+                iconName: "timer"
+                title: "5-hour limit"
+                detail: root.usedPercent >= 0
+                    ? `${root.usedPercent} used · ${root.formatDuration(new Date(root.resetsAt).getTime() - Date.now())} left`
+                    : "Unavailable"
+            }
+
+            LimitRow {
+                Layout.fillWidth: true
+                visible: root.weeklyResetsAt.length > 0
+                iconName: "calendar_month"
+                title: "Weekly limit"
+                detail: root.weeklyUsedPercent >= 0
+                    ? `${root.weeklyUsedPercent} used · ${root.formatDuration(new Date(root.weeklyResetsAt).getTime() - Date.now())} left`
+                    : root.formatDuration(new Date(root.weeklyResetsAt).getTime() - Date.now()) + " left"
+            }
+        }
+    }
+}
diff --git a/modules/ii/settings/pages/BarConfig.qml b/modules/ii/settings/pages/BarConfig.qml
index 2e0a988..421bd8a 100644
--- a/modules/ii/settings/pages/BarConfig.qml
+++ b/modules/ii/settings/pages/BarConfig.qml
@@ -47,6 +47,7 @@ ContentPage {
         { id: "utilButtons",       name: Translation.tr("Util Buttons"),         icon: "toggle_on" },
         { id: "sysTray",           name: Translation.tr("Tray"),                 icon: "inbox" },
         { id: "batteryIndicator",  name: Translation.tr("Battery"),              icon: "battery_android_frame_full" },
+        { id: "codexUsage",        name: Translation.tr("Codex Usage"),          icon: "terminal" },
         { id: "bluetooth",         name: Translation.tr("Bluetooth"),            icon: "bluetooth" },
         { id: "activeWindow",      name: Translation.tr("Active Window"),        icon: "subtitles" },
         { id: "powerButton",       name: Translation.tr("Power Button"),         icon: "power_settings_new" },
